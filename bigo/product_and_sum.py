def productAndSum(array):
    sum = 0 # O(1)
    product = 1 O(1)

    for i in array: # O(n)
        sum += 1 # O(1)

    for i in array: # O(n)
        product += 1 # O(1)

    print("Sum = " + str(sum) + " Product + " + str(product)) # O(1)

    # Has 2 O(n) "for loops" which is O(2n) = O(n)